# Backend Layer

This project uses Supabase as backend.

Backend assets (database schema and migrations) are stored in:

- `backend/supabase/sql/`
- `backend/supabase/migrations/`

Service and UI code are intentionally separated:

- Frontend app: `frontend/`
- Backend data/service layer: `backend/`
